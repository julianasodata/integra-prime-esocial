## Projeto para Felipe

# Integra Prime - Gestão de eSocial e SST

<!--
Para testar a pagina...

Comece verificando se tem o Node.js instalado (node --version) e se a pasta node_modules existe no seu projeto. Se não existir, você precisa rodar npm install antes de tentar iniciar o servidor.

Verificar as dependencias:
1: node --version
2: npm --version

Rodar:
1:
npm run

2:
npm start
ou
npm run dev


1 - push git hub:
git init
git add .
git config --global user.email "Juliana.snto.oliveira@gmail.com"
git commit -m "Initial commit"
git push
-->